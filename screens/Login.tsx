import React from 'react';
import { Button, SafeAreaView } from 'react-native';

export const Login = () => {
    return(
        <SafeAreaView>
              <Button title="Click Me" onPress={() => {}}/>
        </SafeAreaView>
    )
}
